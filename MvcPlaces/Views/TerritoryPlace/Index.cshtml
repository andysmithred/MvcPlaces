@model IEnumerable<MvcPlaces.ViewModels.Models.Main.TerritoryPlaceView>

@{
    ViewData["Title"] = "Index";
}

<br />
<partial name='IndexMenu' model='new { Title = "Territory Places", Icon = "fas fa-map-marked-alt" }' />
<br />

<partial name="PartialListMainTerritoryPlaces" model="@Model.OrderBy(x => x.Place.Name).ToList()" />